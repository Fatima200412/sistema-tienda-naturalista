#include <iostream>
#include "app.h"

using namespace std;

int main() {
    // Crear la aplicación
    app sistema;

    // Iniciar el ciclo principal (login + menú + operaciones)
    sistema.run();

    // Fin del programa
    return 0;
}