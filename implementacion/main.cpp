#include <iostream>
#include "app.h"

using namespace std;

int main() {
    // Crear la aplicacion
    app sistema;

    // Iniciar el ciclo principal (login + menu + operaciones)
    sistema.run();

    // Fin del programa
    return 0;
}